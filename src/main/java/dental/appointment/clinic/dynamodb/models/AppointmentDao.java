package dental.appointment.clinic.dynamodb.models;

public class AppointmentDao {

    // one constructor that we can put an Inject Annotation for DynamoDBMapper
    // will take 2 methods. 1 is for getAppointment(DynamoDBMapper.load) and the other is for saveAppointment()
    //
}
